# requery.js

## 1 什么是 requeryjs

​	在浏览器中可以作为js文件的模块加载器，也就是个加载模块的工具，使用它不需要在html中使用多个标签引入js文件，而是在需要的时候由requeryjs引入。同时可以实现异步加载。

## 2 使用方式

### 1基本api

​	require会定义三个变量：define,require,requirejs，其中require === requirejs，一般使用require更简短	.

​	

​	